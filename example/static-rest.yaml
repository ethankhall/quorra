http:
  - matches:
    - path: /foo/bar
      headers:
        host: localhost
      methods:
       - GET
    response:
      - headers: {}
        body:
          type: json
          json: {}
        status: 200
        weight: 5
      - body:
          type: json
          json: {}
        status: 500
  - matches:
    - path: /graphql
      methods:
       - POST
      graphql:  
        operation-name: coolQuery
    response:
      - headers: {}
        body:
          type: json
          json: {}
        status: 200
